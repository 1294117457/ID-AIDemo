```
1.Zod Schema和后端DTO严格对齐
	字段的完善
2.Zod失效：
	优化prompt
	增加兜底策略
3.think模式使用
	创建不同的model，适配不同的情况选择是否使用think模式
4.RAG升级
	sqlite本地数据库
	X-Request-ID追踪请求
	checkponit清理
	sse增加heartbeat
```

是不是可以引入redis做缓存

```
比如确定主要的几个参数，赛事名称，获奖等级等等
	对应参数的对应申请可以缓存，提高效率
```

