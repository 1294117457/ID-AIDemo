# Agent 工程升级实战：用 LangChain 内置工具替换手写底层

> 目标：把 ID-AIDemo 里手写的文档解析、分块、embedding、向量存储全部换成 LangChain 现成工具，
> 干掉 MCP 那层封装，让代码聚焦在 LangGraph 图编排上。
> 同时告诉你 Java 后端和前端需要跟着改什么。

---

## 改造全景：改了什么、删了什么、不动什么

```
ID-AIDemo 改动：
  删除 ✗                         新增/重写 ✓                    不动 ○
  ─────                          ──────────                     ────
  src/services/docParser.ts      src/services/knowledgeManager.ts   src/agent/state.ts
  src/services/embeddings.ts     （一个文件替代左边 5 个）             src/agent/mainGraph.ts（小改）
  src/services/vectorStore.ts                                       src/agent/subGraphs/applyGraph.ts（小改）
  src/mcp/mcpClient.ts                                              src/agent/subGraphs/consultGraph.ts（小改）
  src/mcp/knowledgeServer.ts                                        src/routes/agent.ts（小改）
  src/seed/seedKnowledge.ts                                         src/routes/config.ts
                                                                    src/services/aiConfig.ts
                                                                    src/db/init.ts（精简）

idbackend 改动：几乎不动，接口契约不变
idfrontend / idfrontend-admin 改动：不动，接口契约不变
```

**核心思路：** 底层换了，但 Express 路由返回的 JSON 格式不变，所以 Java 后端和前端不需要跟着改。

---

## Step 1：安装新依赖，删除不需要的依赖

在 `ID-AIDemo/` 目录下执行：

```bash
# 新增：LangChain 社区加载器、FAISS 向量存储
npm install @langchain/community faiss-node

# 以下是原来就有的，确认存在即可
# @langchain/openai @langchain/langgraph @langchain/core langchain zod

# 可以删除的（不再需要 MCP）：
npm uninstall @langchain/mcp-adapters @modelcontextprotocol/sdk
```

> **为什么选 FAISS？** 本地文件持久化，不需要额外起服务（不像 Chroma 需要单独跑一个进程），适合你这个单机部署的场景。如果你觉得数据量很小不需要持久化，也可以用 `MemoryVectorStore`（纯内存，重启丢失，但最简单）。

> **faiss-node 在 Windows 上装不上怎么办？** 换成 `MemoryVectorStore`，零依赖，后面会给两种写法。

---

## Step 2：写 knowledgeManager.ts — 一个文件替代五个

这是改造的核心。创建 `src/services/knowledgeManager.ts`，用 LangChain 内置工具实现知识库的全部功能。

### 2.1 先看改造前后的对比

```
改造前（5 个文件 + MCP）：
  docParser.ts     → parseFile() 手写了 PDF/DOCX/XLSX 解析
  embeddings.ts    → getEmbedding() 手写了 axios 调 DashScope
  vectorStore.ts   → saveChunk() / searchSimilar() 手写了 SQLite + 余弦搜索
  seedKnowledge.ts → 启动时扫描文件目录入库
  mcp/             → 把上面这些包了一层 MCP 协议

改造后（1 个文件）：
  knowledgeManager.ts → LangChain Loaders + Splitter + Embeddings + VectorStore
```

### 2.2 完整代码

```typescript
// src/services/knowledgeManager.ts

import { OpenAIEmbeddings } from '@langchain/openai'
import { RecursiveCharacterTextSplitter } from 'langchain/text_splitter'
import { MemoryVectorStore } from 'langchain/vectorstores/memory'
import { PDFLoader } from '@langchain/community/document_loaders/fs/pdf'
import { DocxLoader } from '@langchain/community/document_loaders/fs/docx'
import { CSVLoader } from '@langchain/community/document_loaders/fs/csv'
import { TextLoader } from 'langchain/document_loaders/fs/text'
import type { Document } from '@langchain/core/documents'
import { getApiKey, getBaseUrl, getEmbeddingModel } from './aiConfig.js'
import fs from 'fs'
import path from 'path'
import { fileURLToPath } from 'url'

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const KNOWLEDGE_DIR = path.resolve(__dirname, '../../docs/加分文件')

// ─── Embeddings（直接用 LangChain 的，不再手写 axios）───

function createEmbeddings() {
  return new OpenAIEmbeddings({
    openAIApiKey: getApiKey(),
    configuration: { baseURL: getBaseUrl() },
    modelName: getEmbeddingModel(),
  })
}

// ─── Text Splitter（替代手写的 chunkText）───

const splitter = new RecursiveCharacterTextSplitter({
  chunkSize: 500,
  chunkOverlap: 100,
  // 这个比手写的滑动窗口更聪明：
  // 它会优先在 \n\n、\n、句号、空格处切分，而不是硬切 500 字符
})

// ─── Vector Store ───

let vectorStore: MemoryVectorStore | null = null

// 记录已入库的文件（用于列表、删除、去重）
const sourceFileMap = new Map<string, Document[]>()

async function getVectorStore(): Promise<MemoryVectorStore> {
  if (!vectorStore) {
    vectorStore = new MemoryVectorStore(createEmbeddings())
  }
  return vectorStore
}

// ─── 文档加载（替代手写的 docParser.ts）───

async function loadFile(filePath: string, hintExt?: string): Promise<Document[]> {
  const ext = (hintExt ?? path.extname(filePath)).toLowerCase()

  let loader
  switch (ext) {
    case '.pdf':
      loader = new PDFLoader(filePath)
      break
    case '.docx':
    case '.doc':
      loader = new DocxLoader(filePath)
      break
    case '.csv':
      loader = new CSVLoader(filePath)
      break
    case '.xlsx':
    case '.xls':
      // xlsx LangChain 没有直接的 loader，用原来的方式解析后包装成 Document
      const text = await parseXlsx(filePath)
      return text ? [{ pageContent: text, metadata: { source: filePath } }] : []
    case '.md':
    case '.txt':
      loader = new TextLoader(filePath)
      break
    default:
      console.warn(`[knowledge] 不支持的格式: ${ext}`)
      return []
  }

  return loader.load()
}

// xlsx 保留手动解析（LangChain 社区没有好用的 xlsx loader）
async function parseXlsx(filePath: string): Promise<string> {
  const XLSX = await import('xlsx')
  const readFile = (XLSX.default?.readFile ?? XLSX.readFile) as Function
  const workbook = readFile(filePath)
  const lines: string[] = []
  for (const sheetName of workbook.SheetNames) {
    const rows = XLSX.utils.sheet_to_json(workbook.Sheets[sheetName], { header: 1 }) as any[][]
    lines.push(`【${sheetName}】`)
    for (const row of rows) {
      const line = row.map((c: any) => c == null ? '' : String(c)).join('\t').trim()
      if (line) lines.push(line)
    }
  }
  return lines.join('\n')
}

// ─── 对外接口 ───

/**
 * 启动时加载 docs/加分文件/ 下的所有文档（替代 seedKnowledge.ts）
 */
export async function initKnowledge(): Promise<void> {
  if (!fs.existsSync(KNOWLEDGE_DIR)) {
    console.warn('[knowledge] 目录不存在:', KNOWLEDGE_DIR)
    return
  }

  const SUPPORTED = new Set(['.pdf', '.docx', '.doc', '.xlsx', '.xls', '.md', '.txt'])
  const files = fs.readdirSync(KNOWLEDGE_DIR).filter(f => {
    const ext = path.extname(f).toLowerCase()
    return SUPPORTED.has(ext) && !f.startsWith('~$')
  })

  console.log(`[knowledge] 发现 ${files.length} 个文件，开始入库...`)
  const store = await getVectorStore()

  for (const file of files) {
    if (sourceFileMap.has(file)) {
      console.log(`[knowledge] 已跳过: ${file}`)
      continue
    }
    try {
      const docs = await loadFile(path.join(KNOWLEDGE_DIR, file))
      if (docs.length === 0) continue

      // 给每个 document 加上 sourceFile 元数据
      docs.forEach(d => d.metadata.sourceFile = file)
      const chunks = await splitter.splitDocuments(docs)
      chunks.forEach(c => c.metadata.sourceFile = file)

      await store.addDocuments(chunks)
      sourceFileMap.set(file, chunks)
      console.log(`[knowledge] ✓ ${file} → ${chunks.length} 块`)
    } catch (err) {
      console.error(`[knowledge] ✗ ${file}:`, err)
    }
  }
  console.log('[knowledge] 初始化完毕')
}

/**
 * 搜索知识库（替代 MCP 的 search_knowledge）
 */
export async function searchKnowledge(query: string, topK = 5): Promise<string> {
  const store = await getVectorStore()
  const results = await store.similaritySearch(query, topK)

  if (results.length === 0) return '（知识库暂无相关内容）'
  return results
    .map((doc, i) => `[${i + 1}] (${doc.metadata.sourceFile ?? '未知'}) ${doc.pageContent}`)
    .join('\n\n')
}

/**
 * 上传文件到知识库（替代 MCP 的 parse_document + ingest_document）
 */
export async function ingestFile(
  filePath: string, fileName: string, hintExt?: string
): Promise<{ chunkCount: number; textLength: number }> {
  // 先删除同名旧文件
  await removeSource(fileName)

  const docs = await loadFile(filePath, hintExt)
  const fullText = docs.map(d => d.pageContent).join('\n')
  if (!fullText.trim()) {
    return { chunkCount: 0, textLength: 0 }
  }

  docs.forEach(d => d.metadata.sourceFile = fileName)
  const chunks = await splitter.splitDocuments(docs)
  chunks.forEach(c => c.metadata.sourceFile = fileName)

  const store = await getVectorStore()
  await store.addDocuments(chunks)
  sourceFileMap.set(fileName, chunks)

  return { chunkCount: chunks.length, textLength: fullText.length }
}

/**
 * 解析文件为纯文本（给 agent 的 documentText 用，不入库）
 */
export async function parseFileToText(filePath: string, hintExt?: string): Promise<string> {
  const docs = await loadFile(filePath, hintExt)
  return docs.map(d => d.pageContent).join('\n')
}

/**
 * 删除知识库中指定文件
 */
export async function removeSource(sourceFile: string): Promise<void> {
  // MemoryVectorStore 不支持按条件删除，需要重建
  // 如果用 FAISS/Chroma 可以按 metadata 过滤删除
  if (!sourceFileMap.has(sourceFile)) return
  sourceFileMap.delete(sourceFile)
  await rebuildStore()
}

/**
 * 列出所有已入库文件
 */
export function listSources(): { sourceFile: string; chunkCount: number }[] {
  return Array.from(sourceFileMap.entries()).map(([file, chunks]) => ({
    sourceFile: file,
    chunkCount: chunks.length,
  }))
}

/**
 * 统计信息
 */
export function getStats() {
  const sources = listSources()
  const totalChunks = sources.reduce((sum, s) => sum + s.chunkCount, 0)
  return { totalFiles: sources.length, totalChunks, files: sources }
}

// 重建 vector store（删除文件后需要）
async function rebuildStore(): Promise<void> {
  vectorStore = new MemoryVectorStore(createEmbeddings())
  for (const [, chunks] of sourceFileMap) {
    await vectorStore.addDocuments(chunks)
  }
}
```

### 2.3 你在这一步学到了什么

| 手写代码 | LangChain 替代 | 好处 |
|---------|---------------|------|
| `parseFile()` 里的 pdf-parse/mammoth 调用 | `PDFLoader` / `DocxLoader` / `TextLoader` | 一行创建 loader，不用处理 polyfill 兼容性 |
| `chunkText(text, 500, 100)` 滑动窗口 | `RecursiveCharacterTextSplitter` | 更智能的切分（优先在段落/句号处切） |
| `getEmbedding()` 里的 axios 调用 | `OpenAIEmbeddings` | 自动处理截断、批量、重试 |
| `vectorStore.ts` 的 SQLite + 余弦搜索 | `MemoryVectorStore.similaritySearch()` | 一行搜索，不用自己算余弦 |
| MCP Server + Client 整套架构 | 直接调用 | 少了一层进程间通信，代码更简单 |

---

## Step 3：改造 Agent 图节点 — 去掉 MCP 调用

这一步改动很小。原来节点里写 `callTool('search_knowledge', ...)`，现在直接调 `searchKnowledge()`。

### 3.1 改 consultGraph.ts

```diff
 // src/agent/subGraphs/consultGraph.ts

 import { ChatOpenAI } from '@langchain/openai'
 import { ConsultState, ConsultStateType } from '../state.js'
 import { HumanMessage, SystemMessage } from '@langchain/core/messages'
 import { StateGraph, START, END } from '@langchain/langgraph'
-import { callTool } from '../../mcp/mcpClient.js'
+import { searchKnowledge } from '../../services/knowledgeManager.js'
 import { getApiKey, getBaseUrl, getChatModel, getSystemRole } from '../../services/aiConfig.js'

 async function retrieveNode(state: ConsultStateType): Promise<Partial<ConsultStateType>> {
   console.log("--consult:retrieveNode")
   const userMsg = state.messages.filter(m => m instanceof HumanMessage).at(-1)!
   const query = String(userMsg.content)

-  const retrievedContext = await callTool('search_knowledge', { query, topK: 5 })
+  const retrievedContext = await searchKnowledge(query, 5)

   return { retrievedContext }
 }
```

**改了什么：** 一行 import 换了，一行函数调用换了，其他不动。图结构、节点逻辑、state 定义全都不变。

### 3.2 改 applyGraph.ts

```diff
 // src/agent/subGraphs/applyGraph.ts

-import { callTool } from '../../mcp/mcpClient.js'
+import { searchKnowledge } from '../../services/knowledgeManager.js'

 async function fetchPolicyNode(state: ApplyStateType): Promise<Partial<ApplyStateType>> {
   console.log("--apply:fetchPolicy")
-  const policyContext = await callTool('search_knowledge', {
-    query: state.documentText.slice(0, 512),
-    topK: 5,
-  })
+  const policyContext = await searchKnowledge(state.documentText.slice(0, 512), 5)
   return { policyContext }
 }
```

同样，就改了两行。图结构完全不变。

### 3.3 改 routes/agent.ts

```diff
 // src/routes/agent.ts

-import { parseFile } from '../services/docParser.js'
+import { parseFileToText } from '../services/knowledgeManager.js'

 async function parseAgentParams(req: Request) {
   // ...
   if (req.file) {
     const originalName = Buffer.from(req.file.originalname, 'latin1').toString('utf8')
     const hintExt = path.extname(originalName).toLowerCase()
-    documentText = await parseFile(req.file.path, hintExt)
+    documentText = await parseFileToText(req.file.path, hintExt)
     fs.unlink(req.file.path, () => {})
   }
   // ...
 }
```

一行换掉，路由的请求/响应格式完全不变。

### 3.4 改 routes/knowledge.ts

```diff
 // src/routes/knowledge.ts

-import { callTool } from '../mcp/mcpClient.js'
-import { listSources, deleteBySourceFile, totalChunks } from '../services/vectorStore.js'
+import { ingestFile, removeSource, listSources, getStats } from '../services/knowledgeManager.js'

 // GET /knowledge/stats
 router.get('/stats', (_req, res) => {
   try {
-    const sources = listSources()
-    const total = totalChunks()
-    res.json({
-      code: 200, msg: '成功',
-      data: { totalFiles: sources.length, totalChunks: total, files: sources }
-    })
+    res.json({ code: 200, msg: '成功', data: getStats() })
   } catch (err) { /* ... */ }
 })

 // POST /knowledge/upload
 router.post('/upload', upload.single('file'), async (req, res) => {
   // ...
   try {
-    const parseResult = await callTool('parse_document', { filePath, ext: ... })
-    const text = parseResult
-    if (!text.trim() || text === '（文件解析结果为空）') { ... }
-    const ingestResult = await callTool('ingest_document', { sourceFile: fileName, text })
-    const chunkMatch = ingestResult.match(/共 (\d+) 个分块/)
-    const chunkCount = chunkMatch ? parseInt(chunkMatch[1]) : 0
+    const result = await ingestFile(filePath, fileName, ext)
+    if (result.chunkCount === 0) {
+      res.json({ code: 400, msg: '文件内容为空', data: { fileName, status: 'parse_empty' } })
+      return
+    }
     res.json({
       code: 200, msg: '上传成功',
-      data: { fileName, chunkCount, textLength: text.length, status: 'success' }
+      data: { fileName, chunkCount: result.chunkCount, textLength: result.textLength, status: 'success' }
     })
   } catch (err) { /* ... */ }
 })

 // DELETE /knowledge/:sourceFile
 router.delete('/:sourceFile', (req, res) => {
   // ...
-  deleteBySourceFile(sourceFile)
+  await removeSource(sourceFile)
   // ...
 })
```

**响应格式不变**：`{ code, msg, data: { fileName, chunkCount, textLength, status } }` 跟改造前一模一样，所以 Java 后端和前端不用改。

### 3.5 改 index.ts

```diff
 // src/index.ts

-import { seedKnowledge } from './seed/seedKnowledge.js'
-import { closeMcpClient } from './mcp/mcpClient.js'
+import { initKnowledge } from './services/knowledgeManager.js'

 async function main(): Promise<void> {
   console.log('[agent] 启动中...')
   initDb()
-  await seedKnowledge()
+  await initKnowledge()
   app.listen(PORT, () => { /* ... */ })
 }

-process.on('SIGINT', async () => {
-  console.log('\n[agent] 正在关闭...')
-  await closeMcpClient()
-  process.exit(0)
-})
+process.on('SIGINT', () => { process.exit(0) })
```

不再需要 `closeMcpClient()` 了，因为没有 MCP 子进程了。

### 3.6 精简 db/init.ts

`knowledge_chunks` 表不再需要了（向量存在 MemoryVectorStore 内存里）。`conversations` 表如果旧版 `/chat/*` 路由还保留就留着，否则也可以删。`ai_config` 表一定保留。

```diff
 // src/db/init.ts — 只删建表语句，其他不动

 _db.exec(`
-  CREATE TABLE IF NOT EXISTS knowledge_chunks ( ... );
-  CREATE INDEX IF NOT EXISTS idx_chunks_source ON knowledge_chunks(source_file);
-
   CREATE TABLE IF NOT EXISTS conversations ( ... );
   CREATE INDEX IF NOT EXISTS idx_conv_session ON conversations(session_id, created_at);

   CREATE TABLE IF NOT EXISTS ai_config ( ... );
   INSERT OR IGNORE INTO ai_config ...
 `)
```

---

## Step 4：删除不再需要的文件

```
删除：
  src/services/docParser.ts      ← 被 LangChain Loaders 替代
  src/services/embeddings.ts     ← 被 OpenAIEmbeddings 替代
  src/services/vectorStore.ts    ← 被 MemoryVectorStore 替代
  src/mcp/mcpClient.ts           ← 不再需要 MCP
  src/mcp/knowledgeServer.ts     ← 不再需要 MCP
  src/seed/seedKnowledge.ts      ← 被 knowledgeManager.initKnowledge 替代
```

---

## Step 5：确认 Java 后端不用改

检查一遍 idbackend 的代码：

**AIAgentService.java** — 所有方法都是"构造 HTTP 请求 → 转发给 localhost:3001 → 返回响应"。只要 Node 端的路由路径和响应格式不变，这个文件就不用动。

我们改造后：
- `POST /agent/stream` — 路径不变、请求格式不变、SSE 事件格式不变 ✅
- `POST /agent/resume-stream` — 不变 ✅
- `POST /knowledge/upload` — 路径不变、响应 `{code, msg, data: {fileName, chunkCount, ...}}` 不变 ✅
- `GET /knowledge/stats` — 不变 ✅
- `DELETE /knowledge/:sourceFile` — 不变 ✅
- `GET /config` 和 `PUT /config` — 不变 ✅

**AIAgentController.java** — 同理，不用改。

**KnowledgeController.java** — 同理，不用改。

**结论：Java 后端零改动。**

---

## Step 6：确认前端不用改

**idfrontend（学生端）的 `apiAIagent.ts`：**
- `agentStreamChat` 调的是 `/api/agent/stream`，走 Java 后端代理，格式不变 ✅
- `agentResumeStream` 调的是 `/api/agent/resume-stream`，格式不变 ✅

**idfrontend（学生端）的 `ai-agent/index.vue`：**
- `onToken`、`onInterrupt`、`onSession`、`onDone` 回调处理 — Agent 返回的 SSE 事件类型没变 ✅

**idfrontend-admin（管理端）的 `KnowledgePanel.vue`：**
- 调用 `getKnowledgeStats()` → `/api/knowledge/stats` → 返回 `{totalFiles, totalChunks, files}` — 不变 ✅
- 调用 `uploadKnowledge(file)` → `/api/knowledge/upload` → 返回 `{fileName, chunkCount, textLength, status}` — 不变 ✅
- 调用 `deleteKnowledge(sourceFile)` → 不变 ✅

**idfrontend-admin（管理端）的 `AiConfigPanel.vue`：**
- 调用 `getAIConfig()` / `updateAIConfig()` — 不变 ✅

**结论：两个前端零改动。**

---

## Step 7：验证测试

改完之后跑一遍这些测试确认功能正常：

### 7.1 Agent 启动

```bash
cd ID-AIDemo
npm start
```

看控制台日志，应该能看到：
```
[agent] 启动中...
[db] initialized at ...
[knowledge] 发现 N 个文件，开始入库...
[knowledge] ✓ xxx.pdf → 12 块
[knowledge] ✓ xxx.docx → 8 块
[knowledge] 初始化完毕
[agent] 运行中 → http://localhost:3001
```

注意：不再会有 `[MCP client]` 和 `[MCP knowledge-server]` 的日志了。

### 7.2 知识库接口

```bash
# 统计
curl http://localhost:3001/knowledge/stats

# 上传
curl -F "file=@你的文件.pdf" http://localhost:3001/knowledge/upload

# 列表
curl http://localhost:3001/knowledge/list

# 删除
curl -X DELETE http://localhost:3001/knowledge/xxx.pdf
```

### 7.3 Agent 对话

```bash
# 咨询
curl -X POST http://localhost:3001/agent/chat \
  -F "message=挑战杯能加多少分" \
  -F "sessionId=test1"

# 流式
curl -X POST http://localhost:3001/agent/stream \
  -F "message=保研需要什么条件" \
  -F "sessionId=test2"
```

### 7.4 通过 Java 后端调用

启动 idbackend，前端不做任何修改，正常使用浮动 AI 助手，验证：
- 咨询问答正常
- 流式打字效果正常
- 管理端知识库上传/删除正常
- 管理端 AI 配置修改正常

---

## Step 8（可选）：升级到 FAISS 持久化

`MemoryVectorStore` 的缺点是重启丢数据——每次启动都要重新跑 `initKnowledge` 重新向量化所有文件。如果文件多、文件大，启动时间会很长。

用 FAISS 可以把向量索引存到磁盘：

```typescript
// 把 knowledgeManager.ts 里的 MemoryVectorStore 换成 FaissStore

import { FaissStore } from '@langchain/community/vectorstores/faiss'

const FAISS_DIR = path.resolve(__dirname, '../../data/faiss_index')

let vectorStore: FaissStore | null = null

async function getVectorStore(): Promise<FaissStore> {
  if (!vectorStore) {
    // 尝试加载已有索引
    if (fs.existsSync(path.join(FAISS_DIR, 'faiss.index'))) {
      vectorStore = await FaissStore.load(FAISS_DIR, createEmbeddings())
      console.log('[knowledge] 从磁盘加载 FAISS 索引')
    } else {
      vectorStore = new FaissStore(createEmbeddings(), {})
    }
  }
  return vectorStore
}

// 每次 addDocuments 后保存到磁盘
export async function ingestFile(...) {
  // ... 同前 ...
  await store.addDocuments(chunks)
  await store.save(FAISS_DIR)     // 多了这一行
  // ...
}
```

这样重启不用重新向量化，启动时间从几十秒变成几秒。

---

## 改造后的最终文件结构

```
ID-AIDemo/src/
  index.ts                          ← 入口（改了 2 行 import）
  db/
    init.ts                         ← 建表（删了 knowledge_chunks 表）
    mysql.ts                        ← 不动
  services/
    aiConfig.ts                     ← 不动
    knowledgeManager.ts             ← 新文件！替代 5 个旧文件
    ragChain.ts                     ← 旧版聊天，如果还保留的话也要改引用
    analyzeChain.ts                 ← 旧版分析，如果还保留的话也要改引用
  agent/
    state.ts                        ← 不动
    mainGraph.ts                    ← 不动
    subGraphs/
      consultGraph.ts               ← 改了 1 行 import + 1 行调用
      applyGraph.ts                 ← 改了 1 行 import + 1 行调用
  routes/
    agent.ts                        ← 改了 1 行 import + 1 行调用
    knowledge.ts                    ← 改了 import 和上传/删除逻辑
    chat.ts                         ← 如果保留旧版：改引用
    analyze.ts                      ← 如果保留旧版：改引用
    config.ts                       ← 不动
  types/
    scoreTemplate.ts                ← 不动
```

**已删除：**
```
  services/docParser.ts             ✗
  services/embeddings.ts            ✗
  services/vectorStore.ts           ✗
  mcp/mcpClient.ts                  ✗
  mcp/knowledgeServer.ts            ✗
  seed/seedKnowledge.ts             ✗
```

---

## 总结：你在这次升级里搞懂了什么

**之前：** 6 个底层文件 + MCP 协议 → 感觉很复杂

**之后：** 1 个 knowledgeManager.ts，用 LangChain 内置工具 → 代码量减少 70%

**核心认知：**

1. **Agent 开发就应该聚焦在图编排上。** 文档解析、向量存储这些是基础设施，有成熟工具就用成熟工具。

2. **MCP 在这个项目里是过度设计。** 当知识库操作和 Agent 在同一个进程时，直接函数调用比跨进程 MCP 更简单。MCP 更适合的场景是"多个 Agent 共享远程工具"。

3. **接口契约是解耦的关键。** 底层从"手写 SQLite + MCP"换成"LangChain MemoryVectorStore"，但路由返回的 JSON 格式没变，所以 Java 后端和 Vue 前端完全不用动。这就是分层架构的好处。

4. **LangGraph 的节点只关心"输入什么 state、输出什么 state"。** 节点内部用什么工具获取数据是它自己的事。`callTool('search_knowledge')` 还是 `searchKnowledge()` 对图结构没有任何影响。

---

## 附：旧版 ragChain.ts / analyzeChain.ts 的处理

如果你还保留旧版 `/chat/*` 和 `/analyze/*` 路由，它们也引用了 `embeddings.ts` 和 `vectorStore.ts`。两种处理方式：

**方式 A：改引用到 knowledgeManager**

```diff
 // ragChain.ts
-import { getEmbedding } from './embeddings.js'
-import { searchSimilar } from './vectorStore.js'
+import { searchKnowledge } from './knowledgeManager.js'

 async function buildContext(userMessage: string): Promise<string> {
-  const queryVec = await getEmbedding(userMessage)
-  const chunks = searchSimilar(queryVec, 5)
-  return chunks.length > 0
-    ? chunks.map((c, i) => `[${i + 1}] ${c.content}`).join('\n\n')
-    : '（知识库暂无相关内容）'
+  return searchKnowledge(userMessage, 5)
 }
```

**方式 B：直接删掉旧版路由**

旧版 `/chat/*` 和 `/analyze/*` 的功能已经被新版 `/agent/*` 完全覆盖了。如果前端已经全部切到了 `agentStreamChat`，可以直接删掉 `ragChain.ts`、`analyzeChain.ts`、`routes/chat.ts`、`routes/analyze.ts`。

根据你之前的开发文档，前端浮动助手已经用的是 `/api/agent/stream`，所以旧版路由确实可以删了。学生端的"证书分析"（`/api/ai/analyze/certificate`）如果还在用就保留，改引用即可。
